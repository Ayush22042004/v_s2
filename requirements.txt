# Keep your existing libs; these ensure it runs on Render
gunicorn==23.0.0
tzdata==2024.1
# If your app exports Excel, keep openpyxl here; remove if you don't use it
openpyxl==3.1.5
# If your app uses Flask and it's already pinned in your own requirements, delete the line below.
Flask==3.0.3
